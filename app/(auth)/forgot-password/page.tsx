import { RoutePlaceholder } from "@/components/route-placeholder";

export default function ForgotPasswordPage() {
  return <RoutePlaceholder title="Reset your password" description="Password recovery is ready to be connected to the existing authentication flow." />;
}
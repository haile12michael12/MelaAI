import { RoutePlaceholder } from "@/components/route-placeholder";

export default function RegisterPage() {
  return <RoutePlaceholder title="Create your account" description="Registration is ready to be connected to the existing authentication flow." />;
}